include_path = .:/var/www/ext:/var/www/lib:/usr/share/pear:/usr/share/php
auto_prepend_file = Zend/autoload.php

file_uploads = Off

session.auto_start = On
session.name = ticket
session.cookie_httponly = On
session.use_strict_mode = On

display_errors = On
error_reporting = E_ALL
